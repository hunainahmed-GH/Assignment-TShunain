function printFamousquote(): void {
    const quote: string = "The only limit to our realization of tomorrow will be our doubts of today.";
    const author: string = "Franklin D. Roosevelt";

    console.log(`"${quote}" - ${author}`);
}

printFamousQuote();
